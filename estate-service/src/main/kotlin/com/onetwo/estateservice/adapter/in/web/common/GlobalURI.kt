package com.onetwo.estateservice.adapter.`in`.web.common

class GlobalURI {

    companion object {
        const val ROOT_URI = "/estate-service"

        const val ESTATE_RECORD_ROOT = "$ROOT_URI/estates"
    }
}